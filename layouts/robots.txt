User-agent: *
{{- if and (or hugo.IsProduction (eq site.Params.env "production")) (ne site.Params.robotsNoIndex true) }}
Disallow:
{{- else }}
Disallow: /
{{- end }}
Sitemap: {{ "sitemap.xml" | absURL }}
